function setup()
{
  createCanvas(windowWidth,windowHeight);

 // noCursor();
}
function draw()
 {
    background(204);

    text("骨",mouseX, mouseY,);

  translate(50,50);  
  text("犬",mouseX, mouseY,);
}
